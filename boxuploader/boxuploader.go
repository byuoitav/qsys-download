package boxuploader

// "fmt"
// "io"
// "net/http"
// "os"

func UploadFile(filepath string, token string, folderID string) (size int64, err error) {

	//Check for the DSP folder, create if needed
	//loop through folders in the main Box folder and look by name

	//Upload the file

	//delete the file from local storage

	return size, nil
}
